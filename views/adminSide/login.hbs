<!DOCTYPE html>
<html>
<head>
  <title>Admin Login</title>
  <style>
    body {
      background: #1e1e2f;
      font-family: Arial;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: white;
    }

    .login-box {
      background: #2a2a40;
      padding: 30px;
      border-radius: 10px;
      width: 300px;
    }

    input {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
      border: none;
    }

    button {
      width: 100%;
      padding: 10px;
      background: #ff4d4d;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .error {
      color: #ff8080;
      font-size: 14px;
    }
  </style>
</head>
<body>

  <form class="login-box" method="POST" action="/adminSide/login">
    <h2>Admin Login</h2>

    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}

    <input type="email" name="email" placeholder="Admin" />
    <input type="password" name="password" placeholder="Password" />

    <button type="submit" onclick="login()">Login</button>
  </form>

  <script>
    function login(){
      const email = document.querySelector('input[name="email"]').value;
      const password = document.querySelector('input[name="password"]').value;
      if(!email || !password){
        alert('Please fill all fields');
        return;
      }
    }
  </script>
</body>
</html>
